/* The following was run on the-mac.org phpMyAdmin MySQL Database */

CREATE TABLE Device (
    ID			INT 		  NOT NULL,
    DeviceName 	VARCHAR (255) NOT NULL,
    ScreenSize 	VARCHAR (255) NOT NULL,
    OS			VARCHAR (255) NOT NULL,
	OSVersion 	VARCHAR (255) NOT NULL,
    PRIMARY KEY(ID)
);
CREATE TABLE Users (
    ID			INT			NOT NULL,
    UserName    VARCHAR (255) NOT NULL,
    Email      VARCHAR (255) NOT NULL,
    PhoneNumber VARCHAR (255) NOT NULL,
    PRIMARY KEY (ID)
);

CREATE TABLE UsersDevice (
    DeviceID			INT			  NOT NULL,
	UsersID				INT		      NOT NULL,
    UsersUserName    	VARCHAR (255) NOT NULL,
    DeviceDeviceName 	VARCHAR (255) NOT NULL,
	DeviceOS			VARCHAR (255) NOT NULL,
    PRIMARY KEY (DeviceID, UsersID),
    FOREIGN KEY (UsersUserName) REFERENCES Users (UserName) ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY (DeviceDeviceName) REFERENCES Device (DeviceName) ON DELETE CASCADE ON UPDATE CASCADE,
	FOREIGN KEY (DeviceOS) REFERENCES Device(OS) ON DELETE CASCADE ON UPDATE CASCADE,
	FOREIGN KEY (DeviceID) REFERENCES Device(ID) ON DELETE CASCADE ON UPDATE CASCADE,
	FOREIGN KEY (UsersID) REFERENCES Users(ID) ON DELETE CASCADE ON UPDATE CASCADE
);

/* The following was the result of running the Device SQL statement above */
Device

Column		Type		Null	Default		Comments
ID		int(11)		No 	 	 
DeviceName	varchar(255)	No 	 	 
ScreenSize	varchar(255)	No 	 	 
OS		varchar(255)	No 	 	 
OSVersion	varchar(255)	No 	 	

PHP CODE: $sql = "SELECT * FROM `Device` LIMIT 0, 30 ";

/* The following was the result of running the Users SQL statement above */
Users

Column		Type		Null	Default		Comments
ID		int(11)		No 	 	 
UserName	varchar(255)	No 	 	 
Email		varchar(255)	No 	 	 
PhoneNumber	varchar(255)	No 	 	 


PHP CODE: $sql = "SELECT * FROM `Users` LIMIT 0, 30 ";

/* The following was the result of running the UsersDevice SQL statement above */
UsersDevice

Column		 Type		Null	Default		Comments
DeviceID	 int(11)	No 	 	 
UsersID		 int(11)	No 	 	 
UsersUserName	 varchar(255)	No 	 	 
DeviceDeviceName varchar(255)	No 	 	 
DeviceOS	 varchar(255)	No 	 	 


PHP CODE: $sql = "SELECT * FROM `UsersDevice` LIMIT 0, 30 ";
